#include <stdio.h>
/**
 * main - main function
 *
 * Return: returns 0
 */
int main(void)
{
	if (n>0)
		printf("%d is positive\n");
	else if (n>0)
		printf("%d is negative\n");
	else
		printf("0 is zero\n");
	return (0);
}
