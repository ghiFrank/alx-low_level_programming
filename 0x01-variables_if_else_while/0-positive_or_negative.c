#include <stdio.h>
/**
 * main - main function
 *
 * Return: returns 0
 */
int main(void)
{
	if (n>0)
		printf("%d is positive\n",n);
	else if (n>0)
		printf("%d is negative\n",n);
	else
		printf("0 is zero\n");
	return (0);
}
